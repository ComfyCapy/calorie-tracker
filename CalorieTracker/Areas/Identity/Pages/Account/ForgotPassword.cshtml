@page
@model ForgotPasswordModel
@{
    ViewData["Title"] = "Let's get you back in";
}

<h1>@ViewData["Title"]</h1>
<p class="text-body-secondary">Enter your email and we'll send a secure password-reset link if we find a matching account.</p>
<div class="row">
    <div class="col-12 account-form-shell">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
            <div class="form-floating mb-3">
                <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" placeholder="name@example.com" />
                <label asp-for="Input.Email" class="form-label"></label>
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <button type="submit" class="w-100 btn btn-lg btn-primary">Send reset link</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
