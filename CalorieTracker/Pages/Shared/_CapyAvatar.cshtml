@model CalorieTracker.Models.UserCapyAppearance

<div class="capy-avatar">

    <div class="capy-layer" data-capy-layer="@CalorieTracker.Services.CapyCategories.Background">
        @if (Model.Background != null)
        {
            <img src="@Model.Background.ImagePath"
                 alt="" />
        }
    </div>

    <div class="capy-layer" data-capy-layer="@CalorieTracker.Services.CapyCategories.Expression">
        @if (Model.Expression != null)
        {
            <img src="@Model.Expression.ImagePath"
                 alt="Capy avatar" />
        }
    </div>

    <div class="capy-layer" data-capy-layer="@CalorieTracker.Services.CapyCategories.Clothes">
        @if (Model.Clothes != null)
        {
            <img src="@Model.Clothes.ImagePath"
                 alt="" />
        }
    </div>

    <div class="capy-layer" data-capy-layer="@CalorieTracker.Services.CapyCategories.NeckAccessory">
        @if (Model.NeckAccessory != null)
        {
            <img src="@Model.NeckAccessory.ImagePath"
                 alt="" />
        }
    </div>

    <div class="capy-layer" data-capy-layer="@CalorieTracker.Services.CapyCategories.HatHair">
        @if (Model.HatHair != null)
        {
            <img src="@Model.HatHair.ImagePath"
                 alt="" />
        }
    </div>

    <div class="capy-layer" data-capy-layer="@CalorieTracker.Services.CapyCategories.FaceAccessory">
        @if (Model.FaceAccessory != null)
        {
            <img src="@Model.FaceAccessory.ImagePath"
                 alt="" />
        }
    </div>

</div>
