@page
@model CalorieTracker.Pages.Foods.IndexModel

<link rel="stylesheet"
      href="~/react-food-search/assets/food-search.css"
      asp-append-version="true" />

<h1>My Foods</h1>

<form method="get" class="mb-3">
    <input type="hidden"
           asp-for="ReturnToDiary" />

    <input type="hidden"
           asp-for="DiaryDate" />

    <input type="hidden"
           asp-for="DiaryMeal" />

    <div class="input-group">
        <input type="text"
               name="searchTerm"
               value="@Model.SearchTerm"
               class="form-control"
               placeholder="Search my foods..." />

        <button type="submit"
                class="btn btn-outline-primary">
            Search
        </button>
    </div>
</form>

<p>
    <a asp-page="./Create"
       class="btn btn-primary">
        Add Custom Food
    </a>
</p>

@if (!string.IsNullOrWhiteSpace(Model.SearchTerm))
{
    <p>
        Showing results for
        "<strong>@Model.SearchTerm</strong>"

        <a asp-page="./Index"
           asp-route-returnToDiary="@Model.ReturnToDiary"
           asp-route-diaryDate="@Model.DiaryDate?.ToString("yyyy-MM-dd")"
           asp-route-diaryMeal="@Model.DiaryMeal">
            Clear Search
        </a>
    </p>
}


@* =========================
   FAVOURITES
   ========================= *@

@if (Model.FavouriteFoods.Count > 0)
{
    <section class="mt-4">
        <h2>Favourites</h2>

        <div class="food-search-list">
            @foreach (var food in Model.FavouriteFoods)
            {
                <article class="food-search-result">
                    <div class="food-search-result-main">
                        <div>
                            <h3>@food.Name</h3>

                            <p>
                                Per @food.ServingSize.ToString("0.##")@food.ServingUnit
                            </p>
                        </div>

                        <strong class="food-search-calories">
                            @food.Calories kcal
                        </strong>
                    </div>

                    <div class="food-search-result-bottom">
                        <div class="food-search-macros">
                            <span>
                                <strong>P</strong>
                                @food.Protein.ToString("0.##")g
                            </span>

                            <span>
                                <strong>C</strong>
                                @food.Carbohydrates.ToString("0.##")g
                            </span>

                            <span>
                                <strong>F</strong>
                                @food.Fat.ToString("0.##")g
                            </span>
                        </div>

                        <div class="food-search-actions">
                            <form method="post"
                                  asp-page-handler="Unfavourite"
                                  asp-route-id="@food.Id"
                                  asp-route-searchTerm="@Model.SearchTerm"
                                  asp-route-returnToDiary="@Model.ReturnToDiary"
                                  asp-route-diaryDate="@Model.DiaryDate?.ToString("yyyy-MM-dd")"
                                  asp-route-diaryMeal="@Model.DiaryMeal">

                                <button type="submit"
                                        class="food-search-favourite-button is-favourite"
                                        aria-label="Remove from Favourites"
                                        title="Remove from Favourites">
                                    ★
                                </button>
                            </form>

                            @if (food.Source != null)
                            {
                                <a asp-page="./ApiFood"
                                   asp-route-id="@food.ExternalId"
                                   asp-route-date="@(Model.ReturnToDiary ? Model.DiaryDate?.ToString("yyyy-MM-dd") : null)"
                                   asp-route-meal="@(Model.ReturnToDiary ? Model.DiaryMeal : null)"
                                   asp-route-searchTerm="@Model.SearchTerm"
                                   class="btn btn-primary btn-sm">
                                    Add to Diary
                                </a>
                            }
                            else
                            {
                                <a asp-page="../Diary/Create"
                                   asp-route-foodId="@food.Id"
                                   asp-route-date="@(Model.ReturnToDiary ? Model.DiaryDate?.ToString("yyyy-MM-dd") : null)"
                                   asp-route-meal="@(Model.ReturnToDiary ? Model.DiaryMeal : null)"
                                   class="btn btn-primary btn-sm">
                                    Add to Diary
                                </a>

                                <div class="dropdown">
                                    <button class="btn btn-sm btn-outline-secondary food-actions-button"
                                            type="button"
                                            data-bs-toggle="dropdown"
                                            aria-expanded="false"
                                            aria-label="Food actions">
                                        ⋮
                                    </button>

                                    <ul class="dropdown-menu dropdown-menu-end">
                                        <li>
                                            <a asp-page="./Edit"
                                               asp-route-id="@food.Id"
                                               class="dropdown-item">
                                                Edit
                                            </a>
                                        </li>

                                        <li>
                                            <a asp-page="./Portions"
                                               asp-route-id="@food.Id"
                                               class="dropdown-item">
                                                Manage portions
                                            </a>
                                        </li>

                                        <li>
                                            <hr class="dropdown-divider" />
                                        </li>

                                        <li>
                                            <a asp-page="./Delete"
                                               asp-route-id="@food.Id"
                                               class="dropdown-item text-danger">
                                                Delete
                                            </a>
                                        </li>
                                    </ul>
                                </div>
                            }
                        </div>
                    </div>
                </article>
            }
        </div>

        <div class="d-flex justify-content-between align-items-center mt-2">
            <small class="text-muted">
                Showing
                @(((Model.FavouritesPage - 1) * Model.PageSize) + 1)
                –
                @Math.Min(
                    Model.FavouritesPage * Model.PageSize,
                    Model.FavouritesTotalCount)
                of @Model.FavouritesTotalCount
            </small>

            @if (Model.FavouritesTotalPages > 1)
            {
                <nav aria-label="Favourites pages">
                    <ul class="pagination pagination-sm mb-0">
                        <li class="page-item @(Model.FavouritesPage <= 1 ? "disabled" : "")">
                            <a class="page-link"
                               asp-route-searchTerm="@Model.SearchTerm"
                               asp-route-returnToDiary="@Model.ReturnToDiary"
                               asp-route-diaryDate="@Model.DiaryDate?.ToString("yyyy-MM-dd")"
                               asp-route-diaryMeal="@Model.DiaryMeal"
                               asp-route-favouritesPage="@(Model.FavouritesPage - 1)"
                               asp-route-customPage="@Model.CustomPage"
                               asp-route-recentPage="@Model.RecentPage">
                                ‹
                            </a>
                        </li>

                        <li class="page-item disabled">
                            <span class="page-link">
                                Page @Model.FavouritesPage of @Model.FavouritesTotalPages
                            </span>
                        </li>

                        <li class="page-item @(Model.FavouritesPage >= Model.FavouritesTotalPages ? "disabled" : "")">
                            <a class="page-link"
                               asp-route-searchTerm="@Model.SearchTerm"
                               asp-route-returnToDiary="@Model.ReturnToDiary"
                               asp-route-diaryDate="@Model.DiaryDate?.ToString("yyyy-MM-dd")"
                               asp-route-diaryMeal="@Model.DiaryMeal"
                               asp-route-favouritesPage="@(Model.FavouritesPage + 1)"
                               asp-route-customPage="@Model.CustomPage"
                               asp-route-recentPage="@Model.RecentPage">
                                ›
                            </a>
                        </li>
                    </ul>
                </nav>
            }
        </div>
    </section>
}


@* =========================
   CUSTOM FOODS
   ========================= *@

@if (Model.CustomFoods.Count > 0)
{
    <section class="mt-5">
        <h2>Custom Foods</h2>

        <div class="food-search-list">
            @foreach (var food in Model.CustomFoods)
            {
                <article class="food-search-result">
                    <div class="food-search-result-main">
                        <div>
                            <h3>@food.Name</h3>

                            <p>
                                Per @food.ServingSize.ToString("0.##")@food.ServingUnit
                            </p>
                        </div>

                        <strong class="food-search-calories">
                            @food.Calories kcal
                        </strong>
                    </div>

                    <div class="food-search-result-bottom">
                        <div class="food-search-macros">
                            <span>
                                <strong>P</strong>
                                @food.Protein.ToString("0.##")g
                            </span>

                            <span>
                                <strong>C</strong>
                                @food.Carbohydrates.ToString("0.##")g
                            </span>

                            <span>
                                <strong>F</strong>
                                @food.Fat.ToString("0.##")g
                            </span>
                        </div>

                        <div class="food-search-actions">
                            <form method="post"
                                  asp-page-handler="@(food.IsFavourite ? "Unfavourite" : "Favourite")"
                                  asp-route-id="@food.Id"
                                  asp-route-searchTerm="@Model.SearchTerm"
                                  asp-route-returnToDiary="@Model.ReturnToDiary"
                                  asp-route-diaryDate="@Model.DiaryDate?.ToString("yyyy-MM-dd")"
                                  asp-route-diaryMeal="@Model.DiaryMeal">

                                <button type="submit"
                                        class="food-search-favourite-button @(food.IsFavourite ? "is-favourite" : "")"
                                        aria-label="@(food.IsFavourite ? "Remove from Favourites" : "Add to Favourites")"
                                        title="@(food.IsFavourite ? "Remove from Favourites" : "Add to Favourites")">
                                    @(food.IsFavourite ? "★" : "☆")
                                </button>
                            </form>

                            <a asp-page="../Diary/Create"
                               asp-route-foodId="@food.Id"
                               asp-route-date="@(Model.ReturnToDiary ? Model.DiaryDate?.ToString("yyyy-MM-dd") : null)"
                               asp-route-meal="@(Model.ReturnToDiary ? Model.DiaryMeal : null)"
                               class="btn btn-primary btn-sm">
                                Add to Diary
                            </a>

                            <div class="dropdown">
                                <button class="btn btn-sm btn-outline-secondary food-actions-button"
                                        type="button"
                                        data-bs-toggle="dropdown"
                                        aria-expanded="false"
                                        aria-label="Food actions">
                                    ⋮
                                </button>

                                <ul class="dropdown-menu dropdown-menu-end">
                                    <li>
                                        <a asp-page="./Edit"
                                           asp-route-id="@food.Id"
                                           class="dropdown-item">
                                            Edit
                                        </a>
                                    </li>

                                    <li>
                                        <a asp-page="./Portions"
                                           asp-route-id="@food.Id"
                                           class="dropdown-item">
                                            Manage portions
                                        </a>
                                    </li>

                                    <li>
                                        <hr class="dropdown-divider" />
                                    </li>

                                    <li>
                                        <a asp-page="./Delete"
                                           asp-route-id="@food.Id"
                                           class="dropdown-item text-danger">
                                            Delete
                                        </a>
                                    </li>
                                </ul>
                            </div>
                        </div>
                    </div>
                </article>
            }
        </div>

        <div class="d-flex justify-content-between align-items-center mt-2">
            <small class="text-muted">
                Showing
                @(((Model.CustomPage - 1) * Model.PageSize) + 1)
                –
                @Math.Min(
                    Model.CustomPage * Model.PageSize,
                    Model.CustomTotalCount)
                of @Model.CustomTotalCount
            </small>

            @if (Model.CustomTotalPages > 1)
            {
                <nav aria-label="Custom food pages">
                    <ul class="pagination pagination-sm mb-0">
                        <li class="page-item @(Model.CustomPage <= 1 ? "disabled" : "")">
                            <a class="page-link"
                               asp-route-searchTerm="@Model.SearchTerm"
                               asp-route-returnToDiary="@Model.ReturnToDiary"
                               asp-route-diaryDate="@Model.DiaryDate?.ToString("yyyy-MM-dd")"
                               asp-route-diaryMeal="@Model.DiaryMeal"
                               asp-route-favouritesPage="@Model.FavouritesPage"
                               asp-route-customPage="@(Model.CustomPage - 1)"
                               asp-route-recentPage="@Model.RecentPage">
                                ‹
                            </a>
                        </li>

                        <li class="page-item disabled">
                            <span class="page-link">
                                Page @Model.CustomPage of @Model.CustomTotalPages
                            </span>
                        </li>

                        <li class="page-item @(Model.CustomPage >= Model.CustomTotalPages ? "disabled" : "")">
                            <a class="page-link"
                               asp-route-searchTerm="@Model.SearchTerm"
                               asp-route-returnToDiary="@Model.ReturnToDiary"
                               asp-route-diaryDate="@Model.DiaryDate?.ToString("yyyy-MM-dd")"
                               asp-route-diaryMeal="@Model.DiaryMeal"
                               asp-route-favouritesPage="@Model.FavouritesPage"
                               asp-route-customPage="@(Model.CustomPage + 1)"
                               asp-route-recentPage="@Model.RecentPage">
                                ›
                            </a>
                        </li>
                    </ul>
                </nav>
            }
        </div>
    </section>
}


@* =========================
   RECENT FOODS
   ========================= *@

@if (Model.RecentFoods.Count > 0)
{
    <section class="mt-5">
        <h2>Recent Foods</h2>

        <div class="food-search-list">
            @foreach (var food in Model.RecentFoods)
            {
                <article class="food-search-result">
                    <div class="food-search-result-main">
                        <div>
                            <h3>@food.Name</h3>

                            <p>
                                Per @food.ServingSize.ToString("0.##")@food.ServingUnit
                            </p>
                        </div>

                        <strong class="food-search-calories">
                            @food.Calories kcal
                        </strong>
                    </div>

                    <div class="food-search-result-bottom">
                        <div class="food-search-macros">
                            <span>
                                <strong>P</strong>
                                @food.Protein.ToString("0.##")g
                            </span>

                            <span>
                                <strong>C</strong>
                                @food.Carbohydrates.ToString("0.##")g
                            </span>

                            <span>
                                <strong>F</strong>
                                @food.Fat.ToString("0.##")g
                            </span>
                        </div>

                        <div class="food-search-actions">
                            <form method="post"
                                  asp-page-handler="@(food.IsFavourite ? "Unfavourite" : "Favourite")"
                                  asp-route-id="@food.Id"
                                  asp-route-searchTerm="@Model.SearchTerm"
                                  asp-route-returnToDiary="@Model.ReturnToDiary"
                                  asp-route-diaryDate="@Model.DiaryDate?.ToString("yyyy-MM-dd")"
                                  asp-route-diaryMeal="@Model.DiaryMeal">

                                <button type="submit"
                                        class="food-search-favourite-button @(food.IsFavourite ? "is-favourite" : "")"
                                        aria-label="@(food.IsFavourite ? "Remove from Favourites" : "Add to Favourites")"
                                        title="@(food.IsFavourite ? "Remove from Favourites" : "Add to Favourites")">
                                    @(food.IsFavourite ? "★" : "☆")
                                </button>
                            </form>

                            <a asp-page="./ApiFood"
                               asp-route-id="@food.ExternalId"
                               asp-route-date="@(Model.ReturnToDiary ? Model.DiaryDate?.ToString("yyyy-MM-dd") : null)"
                               asp-route-meal="@(Model.ReturnToDiary ? Model.DiaryMeal : null)"
                               asp-route-searchTerm="@Model.SearchTerm"
                               class="btn btn-primary btn-sm">
                                Add to Diary
                            </a>
                        </div>
                    </div>
                </article>
            }
        </div>

        <div class="d-flex justify-content-between align-items-center mt-2">
            <small class="text-muted">
                Showing
                @(((Model.RecentPage - 1) * Model.PageSize) + 1)
                –
                @Math.Min(
                    Model.RecentPage * Model.PageSize,
                    Model.RecentTotalCount)
                of @Model.RecentTotalCount
            </small>

            @if (Model.RecentTotalPages > 1)
            {
                <nav aria-label="Recent food pages">
                    <ul class="pagination pagination-sm mb-0">
                        <li class="page-item @(Model.RecentPage <= 1 ? "disabled" : "")">
                            <a class="page-link"
                               asp-route-searchTerm="@Model.SearchTerm"
                               asp-route-returnToDiary="@Model.ReturnToDiary"
                               asp-route-diaryDate="@Model.DiaryDate?.ToString("yyyy-MM-dd")"
                               asp-route-diaryMeal="@Model.DiaryMeal"
                               asp-route-favouritesPage="@Model.FavouritesPage"
                               asp-route-customPage="@Model.CustomPage"
                               asp-route-recentPage="@(Model.RecentPage - 1)">
                                ‹
                            </a>
                        </li>

                        <li class="page-item disabled">
                            <span class="page-link">
                                Page @Model.RecentPage of @Model.RecentTotalPages
                            </span>
                        </li>

                        <li class="page-item @(Model.RecentPage >= Model.RecentTotalPages ? "disabled" : "")">
                            <a class="page-link"
                               asp-route-searchTerm="@Model.SearchTerm"
                               asp-route-returnToDiary="@Model.ReturnToDiary"
                               asp-route-diaryDate="@Model.DiaryDate?.ToString("yyyy-MM-dd")"
                               asp-route-diaryMeal="@Model.DiaryMeal"
                               asp-route-favouritesPage="@Model.FavouritesPage"
                               asp-route-customPage="@Model.CustomPage"
                               asp-route-recentPage="@(Model.RecentPage + 1)">
                                ›
                            </a>
                        </li>
                    </ul>
                </nav>
            }
        </div>
    </section>
}


@* =========================
   FOOD DATABASE
   ========================= *@

@if (!string.IsNullOrWhiteSpace(Model.SearchTerm))
{
    <div id="react-food-search"
         data-return-to-diary="@Model.ReturnToDiary.ToString().ToLowerInvariant()"
         data-diary-date="@Model.DiaryDate?.ToString("yyyy-MM-dd")"
         data-diary-meal="@Model.DiaryMeal"
         data-initial-search-term="@Model.SearchTerm"
         data-embedded="true">
    </div>
}

@section Scripts {
    <script type="module"
            src="~/react-food-search/assets/food-search.js"
            asp-append-version="true"></script>
}